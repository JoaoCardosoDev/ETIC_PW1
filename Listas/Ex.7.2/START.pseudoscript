#Inverta uma string.

funcao inverter(lista)
    x = tamanho(lista)-1
    lista2 = []
    enquanto  x < 0:
        lista2.adicionar(lista[x])
        x = x - 1
    retorna lista2
    
entrada("Indique uma palavra ou frase a inverter: ", string)

imprime(inverter(string))