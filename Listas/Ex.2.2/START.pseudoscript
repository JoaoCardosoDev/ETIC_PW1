#Preencha um array com valores fornecidos pelo usuário.

funcao adicionar(x, y)
    x = []
    counter = 0
    enquanto counter < y:
        entrada("Indique número a adicionar: ", array[counter])
        counter++

entrada("Indique o nome do array: ", x)
entrada("Indique o número de slots: ", y)

adicionar(x, y)