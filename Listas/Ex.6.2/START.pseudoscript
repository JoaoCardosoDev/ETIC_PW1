#Implemente um algoritmo de pesquisa (por exemplo, Linear Search) num array.

funcao linear(lista, a):
    por cada i de 0 a lista.tamanho():
        se lista[i] == a
            retorna i 
    

listaexemplo = [ 1, 3, 4, 6, 7, 9]

entrada("indique o nome que procura na lista: ", numero)

se linear(listaexemplo, numero):
    imprime("O {numero} está na posição {linear(listaexemplo, numero)}")
senao:
    imprime("Número não está na lista.")
